import 'package:flutter/material.dart';

class TaksPage extends StatelessWidget {
  const TaksPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Task Page'),
      ),
    );
  }
}
